const multiplyTwoNumbers = (a, b) => a * b;
export default multiplyTwoNumbers;

export const multiplyThreeNumbers = (a, b, c) => {
  return a * b * c;
};

export const multiplyFourNumbers = (a, b, c, d) => {
  return a * b * c * d;
};
