const divideTwoNumbers = (a, b) => a / b;
export default divideTwoNumbers;

export const divideThreeNumbers = (a, b, c) => {
  return a / b / c;
};

export const divideFourNumbers = (a, b, c, d) => {
  return a / b / c / d;
};
